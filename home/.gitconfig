[user]
	name = Sebastian Peters
	email = koelnconcert@googlemail.com
[core]
#	autocrlf = input
[log]
	decorate = short
	date = local
[color]
	status = always
	ui = always
[push]
	default = simple
[core]
 	pager = diff-so-fancy | less --tabs=4 -RFX
[svn]
	rmdir = true
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
